# GitHub Personal Access Token (Classic)
# Required for fetching contribution graph via GraphQL API
# 
# How to create:
# 1. Go to https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name (e.g., "GitHub Profile App")
# 4. Select scopes: (none needed for public data)
# 5. Click "Generate token"
# 6. Copy the token and paste it here
#
# Note: The token is optional. Without it, the app will use mock contribution data.

NEXT_PUBLIC_GITHUB_TOKEN=your_github_token_here
